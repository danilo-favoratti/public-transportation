<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="AppHeading" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:padding">@dimen/padding_medium</item>
        <item name="android:background">@color/colorBackgroundTitle</item>
        <item name="android:textSize">@dimen/font_heading</item>
        <item name="android:ellipsize">end</item>
        <item name="android:singleLine">true</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="AppSubHeading">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:paddingTop">@dimen/padding_medium</item>
        <item name="android:paddingBottom">@dimen/padding_medium</item>
        <item name="android:paddingRight">@dimen/padding_medium</item>
        <item name="android:paddingLeft">@dimen/padding_big</item>
        <item name="android:textSize">@dimen/font_sub_heading</item>
        <item name="android:textColor">@color/colorSubHeading</item>
    </style>

    <style name="AppTitle" parent="AppSubHeading">
        <item name="android:paddingTop">@dimen/padding_bigger</item>
        <item name="android:paddingLeft">@dimen/padding_bigger</item>
        <item name="android:textSize">@dimen/font_title</item>
        <item name="android:textColor">@color/colorTitle</item>
    </style>

    <style name="FlatButton" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:gravity">right</item>
    </style>

</resources>
